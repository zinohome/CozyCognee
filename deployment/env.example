# Cognee 环境变量配置示例
# 复制此文件为 .env 并修改相应的配置值

# ==================== 基础配置 ====================
DEBUG=false
ENVIRONMENT=local
LOG_LEVEL=ERROR
HOST=0.0.0.0

# ==================== LLM 配置 ====================
# 必需：配置您的LLM API密钥
LLM_API_KEY=your-llm-api-key-here
LLM_PROVIDER=openai  # 可选: openai, anthropic, groq, ollama, mistral
LLM_MODEL=gpt-4o-mini  # 根据provider选择模型

# ==================== 数据库配置 ====================
# 关系型数据库配置
DB_PROVIDER=sqlite  # 可选: sqlite, postgres
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cognee_db
DB_USERNAME=cognee
DB_PASSWORD=cognee

# ==================== 向量数据库配置 ====================
# 可选: pgvector (推荐), chroma, qdrant, weaviate, pinecone
# pgvector 使用 PostgreSQL 的扩展，无需额外服务
VECTOR_DB_PROVIDER=pgvector  # 使用 PostgreSQL 的 pgvector 扩展
# 注意: 使用 pgvector 时，向量数据存储在 PostgreSQL 中，无需 VECTOR_DB_URL 和 VECTOR_DB_KEY
# 如果使用其他向量数据库，需要配置相应的 URL 和 KEY

# ==================== 图数据库配置 ====================
GRAPH_DATABASE_PROVIDER=kuzu  # 可选: kuzu, neo4j
NEO4J_URL=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=pleaseletmein

# ==================== MCP 配置 ====================
TRANSPORT_MODE=sse  # 可选: stdio, sse, http
MCP_LOG_LEVEL=INFO

# ==================== CORS 配置 ====================
# 允许的跨域来源（逗号分隔）
# 如果不设置，默认只允许 UI_APP_URL（默认 http://localhost:3000）
# CORS_ALLOWED_ORIGINS=http://192.168.66.11:8000,http://192.168.66.11:3000,http://localhost:3000
# 或者允许所有来源（不推荐用于生产环境）
# CORS_ALLOWED_ORIGINS=*

# ==================== 其他配置 ====================
# 可选依赖
EXTRAS=api,postgres,neo4j

# API模式配置（用于MCP连接到远程API）
# API_URL=http://cognee:8000
# API_TOKEN=your-api-token

